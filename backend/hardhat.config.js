require('@nomiclabs/hardhat-waffle');

module.exports = {
  solidity: '0.8.0',
  networks: {
    rinkeby: {
      url: '', // removed for privacy
      accounts: [], // removed for privacy
    },
  },
};